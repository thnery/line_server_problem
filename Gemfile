source 'https://rubygems.org'

ruby '3.4.2'

# using sinatra for main web framework
gem 'sinatra'
gem 'sinatra-contrib'

# redis
gem 'redis'

# puma as the main application server
gem 'puma'

gem 'rackup', '~> 2.2'

group :development, :test do
  gem 'rerun'
  gem 'byebug', '~> 11.1', '>= 11.1.3'
end
